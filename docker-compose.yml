services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: bloodlink
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - bloodlink-postgres:/var/lib/postgresql/data
volumes:
  bloodlink-postgres:
